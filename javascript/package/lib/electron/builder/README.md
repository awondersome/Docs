# [builder](https://www.electron.build/)

优化了官方的packager

- `yarn add electron-builder --dev`
- ```
    "build": {
        "appId": "your.id",
        "mac": {
            "category": "your.app.category.type"
        }
    }
  ```